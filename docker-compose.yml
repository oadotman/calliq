# =====================================================
# DOCKER COMPOSE FOR LOCAL DEVELOPMENT
# For production on Datalix, use standalone Docker
# =====================================================

version: '3.8'

services:
  synqall:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: synqall-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      # Add all environment variables from .env file
    env_file:
      - .env.production
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - synqall-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  synqall-network:
    driver: bridge
